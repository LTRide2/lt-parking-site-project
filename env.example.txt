# Copy to .env to override defaults. The app works with NO .env (mock is on by default).
# VITE_API_URL  — base URL of a real backend (used only when VITE_USE_MOCK=false)
# VITE_USE_MOCK — "true" (default) uses the in-memory mock backend; "false" hits VITE_API_URL
VITE_API_URL=http://localhost:8000
VITE_USE_MOCK=true
